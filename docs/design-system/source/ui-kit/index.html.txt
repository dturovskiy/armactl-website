<!-- @dsCard group="Website" viewport="1280x800" name="armactl.dev" subtitle="Marketing site recreation — hero, capabilities, philosophy, footer" -->
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>armactl — Arma Reforger Server Manager</title>
<template id="__bundler_thumbnail" data-bg-color="#121214">
  <svg viewBox="0 0 1200 800" xmlns="http://www.w3.org/2000/svg">
    <rect width="1200" height="800" fill="#121214"/>
    <text x="600" y="430" font-family="monospace" font-size="150" font-weight="700" fill="#00FF94" text-anchor="middle">&gt;_</text>
    <text x="600" y="540" font-family="monospace" font-size="60" fill="#00B2FF" text-anchor="middle">armactl</text>
  </svg>
</template>
<link rel="stylesheet" href="../../styles.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<script src="https://unpkg.com/react@18.3.1/umd/react.development.js" integrity="sha384-hD6/rw4ppMLGNu3tX5cjIb+uRZ7UkRJ6BPkLpg4hAu/6onKUg4lLsHAs9EBPT82L" crossorigin="anonymous"></script>
<script src="https://unpkg.com/react-dom@18.3.1/umd/react-dom.development.js" integrity="sha384-u6aeetuaXnQ38mYT8rp6sbXaQe3NL9t+IBXmnYxwkUI2Hw4bsp2Wvmx4yRQF1uAm" crossorigin="anonymous"></script>
<script src="https://unpkg.com/@babel/standalone@7.29.0/babel.min.js" integrity="sha384-m08KidiNqLdpJqLq95G/LEi8Qvjl/xUYll3QILypMoQ65QorJ9Lvtp2RXYGBFj1y" crossorigin="anonymous"></script>
<script src="../../_ds_bundle.js"></script>
<style>
  html, body { margin: 0; }
  body { background-color: var(--bg-dark); }
  /* Site-level layout styles (lifted from website/index.html) */
  .site { font-family: var(--font-mono); background-color: var(--bg-dark); color: var(--text-main); line-height: var(--leading-base); -webkit-font-smoothing: antialiased; }
  .container { width: 100%; max-width: var(--container-max); margin: 0 auto; padding: 0 var(--container-pad); box-sizing: border-box; }
  .site section { padding: var(--space-section) 0; position: relative; }
  .site a { color: var(--link-color); text-decoration: none; transition: color var(--transition-base); }
  .site a:hover { color: var(--link-hover); }
  /* Header */
  .site-header { position: sticky; top: 0; height: var(--header-height); background-color: var(--header-bg); backdrop-filter: blur(10px); border-bottom: var(--border-panel); z-index: 300; display: flex; align-items: center; }
  .nav-container { display: flex; justify-content: space-between; align-items: center; width: 100%; }
  .logo-container { display: flex; align-items: center; gap: 15px; }
  .logo-img { width: 45px; height: 45px; object-fit: contain; }
  .logo-text { font-family: var(--font-display); font-size: var(--text-xl); color: var(--heading-color); letter-spacing: var(--tracking-display); }
  .nav-links { display: flex; gap: 2rem; list-style: none; margin: 0; padding: 0; }
  .nav-links a { font-size: var(--text-sm); text-transform: uppercase; color: var(--text-muted); letter-spacing: var(--tracking-wide); position: relative; }
  .nav-links a::after { content: ''; position: absolute; width: 0; height: 1px; bottom: -5px; left: 0; background-color: var(--neon-blue); transition: width var(--transition-base); }
  .nav-links a:hover { color: var(--text-main); }
  .nav-links a:hover::after { width: 100%; }
  .nav-actions { display: flex; align-items: center; gap: 1.5rem; }
  .theme-toggle-btn { cursor: pointer; color: var(--text-muted); font-size: 1.1rem; transition: color var(--transition-base); background: none; border: none; padding: 0; }
  .theme-toggle-btn:hover { color: var(--neon-orange); }
  /* Hero */
  .hero { min-height: calc(100vh - var(--header-height)); display: flex; align-items: center; background: linear-gradient(135deg, var(--bg-dark) 0%, var(--bg-darker) 100%); border-bottom: var(--border-panel); position: relative; overflow: hidden; }
  .hero::before { content: ''; position: absolute; top: 0; right: 0; width: 50%; height: 100%; background: radial-gradient(circle at 70% 50%, var(--neon-blue-alpha-05) 0%, transparent 60%); pointer-events: none; }
  .hero-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center; position: relative; }
  .hero h1 { font-family: var(--font-display); font-weight: normal; font-size: var(--text-hero); line-height: var(--leading-tight); margin: 0 0 1.5rem 0; color: var(--heading-color); letter-spacing: var(--tracking-wide); }
  .terminal-prompt { color: var(--neon-green); margin-right: 10px; }
  .hero-lead { font-size: var(--text-lg); color: var(--text-muted); max-width: 90%; font-weight: var(--weight-light); }
  /* Sections */
  .overview { background-color: var(--bg-dark); border-bottom: var(--border-panel); }
  .overview-box { background-color: var(--box-bg); border-left: var(--border-accent-width) solid var(--neon-green); padding: var(--space-6); font-size: var(--text-md); }
  .overview-box p { font-weight: var(--weight-light); margin: 0 0 1.5rem 0; }
  .overview-box p:last-child { margin-bottom: 0; }
  .features { background-color: var(--bg-darker); }
  .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: var(--space-6); }
  .image-break { height: 400px; width: 100%; position: relative; border-top: var(--border-panel); border-bottom: var(--border-panel); background-size: cover; background-position: center; background-attachment: fixed; }
  .image-break::after { content: ''; position: absolute; inset: 0; background: linear-gradient(90deg, var(--bg-darker) 0%, transparent 20%, transparent 80%, var(--bg-darker) 100%); pointer-events: none; }
  .values { background-color: var(--bg-dark); }
  .values-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: var(--space-6); border-top: var(--border-panel); padding-top: var(--space-8); }
  /* Footer */
  .site-footer { background-color: var(--bg-darker); border-top: var(--border-panel); padding: var(--space-8) 0 var(--space-6) 0; }
  .footer-content { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: var(--space-6); margin-bottom: 3rem; }
  .social-links { display: flex; gap: 1.5rem; align-items: center; }
  .social-links a { color: var(--text-muted); font-size: var(--text-xl); transition: color var(--transition-base), transform var(--transition-base); }
  .social-links a:hover { color: var(--neon-blue); transform: var(--hover-nudge); }
  .footer-bottom { text-align: center; padding-top: var(--space-6); border-top: var(--border-hairline); color: var(--text-muted); font-size: var(--text-xs); }
  .footer-bottom p { margin: 0; }
</style>
</head>
<body>
<div id="root"></div>
<script type="text/babel" src="website-app.jsx"></script>
</body>
</html>
