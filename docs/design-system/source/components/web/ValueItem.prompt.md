Philosophy/values list item — big translucent orange number behind an orange uppercase title.

```jsx
<ValueItem number="01" title="Idempotent Operations">
  Run it twice, get the same result. Every action detects current state first.
</ValueItem>
```
